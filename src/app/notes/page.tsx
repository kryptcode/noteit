import { Metadata } from "next"

export const metadata : Metadata = {
  title: "NoteIt - Notes"
}

const NotesPage = () => {
  return (
    <div>
        NotesPage
    </div>
  )
}

export default NotesPage